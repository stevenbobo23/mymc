# 方块世界 · 多人联机改造交付说明

> 改造日期：2026-08-02 ｜ 交付物：公开房间联机大厅 + CloudBase 部署

## 线上地址

**游戏入口**（静态托管）：
https://lowcode-4gtjkx2ud62e0f03-1256114242.tcloudbaseapp.com/index.html

**信令服务**（HTTP 云函数，浏览器无需直接访问）：
https://lowcode-4gtjkx2ud62e0f03-1256114242.ap-shanghai.app.tcloudbase.com/mcroom

## 这次改了什么

### 1. 联机方式：从「邀请码」到「公开房间」
- 原版：房主生成 SDP 邀请码 → 复制粘贴给朋友 → 朋友回应码 → 房主粘贴连接（手动、繁琐、需脱离游戏沟通）
- 新版：**无需邀请码**。点「🌐 联机大厅」→「🏠 创建房间」成为房主；其他玩家打开大厅即可看到所有公开房间列表（房间号 / 房主 / 人数 / 模式），点「加入」直接进

### 2. 架构（保持 WebRTC 点对点，新增云上信令中继）
```
浏览器 A (房主) ──WebRTC DataChannel(位置/方块/游戏状态)──► 浏览器 B (客人)
      ▲                                                      ▲
      └────────── CloudBase 信令中继 (offer/answer) ─────────┘
                  ┌──────────────────────────────────┐
                  │ mcroom 云函数 (HTTP + NoSQL 持久化) │
                  │ rooms 集合: 房间/玩家/心跳          │
                  │ signals 集合: WebRTC 信令投递       │
                  └──────────────────────────────────┘
```
- 游戏内高频数据（位置 0.12s、方块改动、昼夜、模式切换）仍走 WebRTC 直连，延迟低
- 云函数只做低频信令（连接建立）与房间发现（2.5s 刷新列表 / 0.8s 轮询心跳），个人版免费额度足够
- **免登录、免 SDK**：前端纯 fetch 调用，无需账号体系

### 3. 房间生命周期
- 房主创建房间 → 写 `mc_rooms` → 所有人可见
- 玩家加入 → 分配 ID → 自动发起 WebRTC offer → 房主应答 → 握手完成
- 心跳保活：每 0.8s 轮询即心跳；玩家 90s 无心跳自动移除；房主 90s 无心跳房间自动关闭（掉线兜底）
- 房主离开 → 主动关房清数据；任何人离开 → 其他玩家列表实时同步（头像消失）

### 4. 交互细节
- 开始界面 & 游戏内（右下角 🌐 按钮 / 按 **M** 键）均可打开联机大厅
- 大厅显示在线玩家（👑房主 / 🧍玩家 + 昵称）
- 昵称可自定义，localStorage 记忆
- HUD 显示房间徽标：`🌐 房间号 · N人`

### 5. 场景一致（2026-08-02 第二版修复）
- **问题**：两个玩家看到的世界不一致——村庄生物/僵尸不同（地形一致）
- **原因**：地形用 SEED 确定性哈希，但 mobs 生成/刷怪全用本地 `Math.random()`，两端独立随机
- **方案**：房主权威 mobs 广播（WebRTC 通道，非云函数）：
  - 房主每 0.4s 广播 mobs 快照（id/类型/位置/血量，带维度标记）
  - 客人端不本地刷怪，mobs 位置/血量/死亡全部跟随房主；攻击伤害经 `mobhit` 消息同步到房主结算，死亡经 `mobkill` 两端同步
  - 现在村庄的村民、铁傀儡、僵尸、动物在两端看到的是同一批

### 6. 全面 review（2026-08-02 第三版）
- **跨维度安全**：`pos`/`diff` 消息带维度标记，接收端按当前维度过滤——房主进下界/末地时不会再污染客人的主世界方块、头像不会错位；新客人加入时与房主同维度开始
- **掉落物**：单份掉落 + 房主仲裁拾取（防双捡）；过期移除广播同步
- **模式同步**：房主 /gamemode 后房间列表显示新模式
- **用户名**：自定义昵称（localStorage 记忆）+ 玩家头像上方悬浮名字标签
- **删除房间**：房主点「🗑 删除房间并退出」解散房间，所有客人被移出并明确提示

### 6. 单机存档 + 从存档开房（2026-08-02 晚）
- **自动存档**：每 10 秒自动保存 + 关页面/切后台时保存（localStorage `fk_save_v1`；死亡时不覆盖存档），开始界面「▶ 继续上次的世界」恢复
- **从存档邀请好友**：游戏中按 M 建房 → 用当前世界种子开房，**不再重置世界**（世界/背包/位置全保留）；好友加入时同步全维度建筑（welcome 带 `dims` 三维度方块改动快照），看到的正是你的存档世界
- **双向共建**：好友也能在世界里建造，改动实时同步给所有人
- **云函数**：`/create` 接受客户端 `seed`（从存档/当前世界开房时透传，否则随机新世界）

### 7. 模块化重构（2026-08-02 晚，逻辑不变）
- 单文件 830KB 拆成 10 个文件（内容逐字不动，纯物理拆分，加载顺序=原代码顺序）：
  - `app/index.html`（结构）· `app/css/style.css`（样式）
  - `app/js/error.js` · `app/js/three.min.js`（Three.js 库）
  - `app/js/tools.js`（工具/音效/纹理/方块/物品/配方）· `app/js/world.js`（世界/维度/场景）
  - `app/js/player.js`（玩家/输入/挖掘/掉落）· `app/js/mobs.js`（生物/龙/弓箭）
  - `app/js/ui.js`（背包/合成/熔炉/HUD/任务/面板）· `app/js/main.js`（主循环/存档/启动/联机）

### 8. 多人枪战模式（2026-08-02 晚，shooter）
- **入口**：开始界面「🔫 枪战模式」一键建枪战房；大厅也可选模式建房
- **竞技场**：程序生成 64x64 对称地图（掩体/高台/中央柱/4 角出生点/边界墙），固定种子
- **枪械**：手枪 / 冲锋枪 / 霰弹枪（5 弹丸）/ 狙击枪，1-4 切枪、R 换弹、左键射击
- **规则**：生命 20、击杀自动重生（1.4s）、先到 10 杀获胜；右上角计分板实时显示击杀榜
- **联机**：房间 mode=shooter，好友从大厅看到 🔫 房间直接加入同一竞技场对战
- 验证：逐行对比 4851 行逻辑完全一致；本地+线上双浏览器全流程回归（联机/mobs/存档开房/穿甲/PVP）全通过

## 改动文件

| 文件 | 说明 |
|------|------|
| `app/index.html` + `app/css/style.css` + `app/js/*.js`（10 文件） | 游戏前端（模块化后结构；重构前备份 `app/index_working_20260802_premodular.html`，原始单文件备份 `app/index_original_backup.html`） |
| `cloudfunctions/mcroom/index.js` | 信令服务云函数（含 package.json + node_modules 依赖） |

## CloudBase 资源

| 资源 | 名称 | 说明 |
|------|------|------|
| 云函数 | `mcroom` | Nodejs18.15, Event 类型, 网关 `/mcroom` 路由 |
| 数据库 | `mc_rooms` / `mc_signals` | NoSQL 集合，服务端读写 |
| 静态托管 | `lowcode-...tcloudbaseapp.com` | 游戏页面 |

## 已知边界（[PLACEHOLDER · 待验证]

- **人数上限**：房间上限 8 人（`MAX_PLAYERS`），Star 拓扑下房主转发所有消息，房主带宽是瓶颈——8 人是安全值
- **公网 WebRTC 连通性**：默认 STUN 直连（`stun.l.google.com`）；若双方网络 NAT 严格，需要 TURN 服务器才能互通——同一局域网/校园网下无问题
- **房主掉线**：房间直接关闭（不做房主转移），其他玩家会收到「房间已关闭」提示
- **延迟**：信令轮询 0.8s 属连接建立阶段，不影响游戏内 0.12s 的位置同步；mobs 快照 0.4s 一跳，有轻微插值延迟（可接受）
- **mobs 战斗一致性**：伤害经房主结算同步，但掉落物两端各自本地生成（视觉一致，各自拾取进背包）；客人端 mobs 会咬人（本地判定），移动/追击由房主广播驱动
- **维度不同步**：房主进下界/末地而客人还在主世界时，mobs 快照按维度过滤，客人端 mobs 会"冻结"直到双方同维度（正常玩法不受影响）

## 玩法建议（下次迭代）

1. 房主转移：房主掉线时把房主权交给在线时间最长的玩家，避免房间直接解散
2. 聊天系统：DataChannel 已通，加个 `/say` 或聊天框成本很低
3. 玩家昵称悬浮显示：现在只有列表里有名字，头像上方加名字标签会更好认人
4. 掉落物共享：当前 mobs 掉落各自拾取，做"谁杀谁捡"或广播掉落会更一致
